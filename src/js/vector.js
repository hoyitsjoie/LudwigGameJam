export const vec2 = (x = 0, y = 0) => ({ x: x, y: y });
export const vec3 = (x = 0, y = 0, z = 0) => ({ x: x, y: y, z: z });
export const vec4 = (x = 0, y = 0, z = 0, w = 0) => ({
  x: x,
  y: y,
  z: z,
  w: w,
});
